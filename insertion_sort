//삽입정렬

//삽입정렬은 기본적으로 정렬되지 않은 배열과 key값을 입력으로 받는다.
//그리고 정렬되지 않은 배열에서 제일 첫번째 값을 제외하고 나머지 숫자들에서 key값을 골라 그 첫번째값과 비교하여 정렬하는 방법이다.
//첫번째 값은 정렬된 배열이라고 가정한다. 따라서 key값과 정렬된 배열간의 비교이기도 하다.


package com.company;

public class Main {

    public static int [] insertion_Sort(int[] array) {
        for (int j = 1; j<array.length ; j++) {
            int i = j-1;
            int key = array[j];
            while(i>=0 && (array[i] > key)) {
                array[i+1] = array[i];
                array[i] = key;
                i -= 1;
            }
        }
        return array;
    }

    public static void print_Sort(int [] array) {
        for(Integer number : array) {
            System.out.println(number);
        }
    }


    public static void main(String[] args) {

        int [] array = {4, 23, 6, 78, 1, 54, 231, 9, 12};
        print_Sort(insertion_Sort(array));

    }
}
