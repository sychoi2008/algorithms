# 선택정렬

# 선택정렬은 정렬되지 않은 배열을 입력으로 받는다.
# 정렬되지 않은 배열에서 최소값을 선택후 그 값을 맨 앞으로 가져간다. 맨 앞으로 가져간 최소값은 정렬된 배열이 된다.
# 그 다음은 정렬된 배열을 제외한 나머지 값들 중에서 최소값을 고른 후 그것을 정렬되지 않은 배열의 맨 앞으로 보낸다. 

package com.company;

public class Main {


    public static int [] selection_Sort(int count, int[] array) {

        int j = count;
        int min = array[count];
        for(int i= count; i<array.length; i++) {
            if (min > array[i]) {
                min = array[i];
                j = i;
            }
        }
        array[j] = array[count];
        array[count] = min;
        return array;
    }

    public static void print_Sort(int [] array) {
        for(Integer number : array) {
            System.out.println(number);
        }
    }


    public static void main(String[] args) {

        int [] array = {4, 23, 6, 78, 1, 54, 231, 9, 12};

        for(int i = 0; i<array.length; i++) {
            selection_Sort(i, array);
        }
        print_Sort(array);

    }
}
